FROM msc24x/showdown:latest-compilers AS server

ENV APP_DIR /showdown
WORKDIR $APP_DIR

RUN adduser showdown \
    && echo "showdown ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers

ADD . .

RUN chown -R showdown:1000 $APP_DIR
ENV PATH=$PATH$COMPILERS_PATH

USER showdown

CMD ["./scripts/init_dev.sh"]